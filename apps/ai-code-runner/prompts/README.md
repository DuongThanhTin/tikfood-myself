# Runner Prompts

The canonical runtime prompts live in `packages/prompts/` at the workspace root.

This folder exists for runner-specific prompt overrides or copied prompt bundles if a future Docker build needs them inside the service image.
